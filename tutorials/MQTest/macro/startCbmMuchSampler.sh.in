#!/bin/bash


buffSize="1000" # zeromq high-water mark is in messages
dataFormat="Boost"

# MUCH SAMPLER

ID="201"
inputFile="@CMAKE_SOURCE_DIR@/tutorials/MQTest/macro/data/test.raw.root"
parameterFile="@CMAKE_SOURCE_DIR@/tutorials/MQTest/macro/data/params.root"
branch=$1
eventRate="0"
numIoThreads="1"
outputSocketType="push"
outputBufSize=$buffSize
outputMethod="connect"
outputAddress="tcp://depc310:5567"

#xterm -hold -e @CMAKE_BINARY_DIR@/bin/testCbmMuchDetectorSampler_$dataFormat $ID $inputFile $parameterFile $branch $eventRate $numIoThreads $outputSocketType $outputBufSize $outputMethod $outputAddress &
@CMAKE_BINARY_DIR@/bin/testCbmMuchDetectorSampler_$dataFormat $ID $inputFile $parameterFile $branch $eventRate $numIoThreads $outputSocketType $outputBufSize $outputMethod $outputAddress
